name: sga_app
description: Sistema de Gestión Agrícola - SGA App
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

  # HTTP Client
  dio: ^5.4.0

  # Local storage (tokens, sesión)
  shared_preferences: ^2.2.2

  # State management
  provider: ^6.1.1

  # Utilities
  intl: ^0.19.0

  # ── NUEVO: Cámara y galería ──────────────────────────────────────────────
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
